.blocks.filter
	label(
	for='showfilter'
	id='showfilterlabel'
	)
		| trier et filtrer
	input(
	type='checkbox'
	id='showfilter'
	)
	.my_container
		.my_tab
			- $terms = get_terms( 'bieres_pays' );
			- $labelslug = 'bieres_pays';
			- $label = (ICL_LANGUAGE_CODE == 'nl') ? 'Land' : 'Pays';
			include select.php.jade
			- $terms = get_terms( 'bieres_type' );
			- $labelslug = 'bieres_type';
			- $label = (ICL_LANGUAGE_CODE == 'nl') ? 'Stijl' : 'Style';
			include select.php.jade
			- $terms = get_terms( 'bieres_couleur' , 'orderby=term_order');
			- $labelslug = 'bieres_couleur';
			- $label = (ICL_LANGUAGE_CODE == 'nl') ? 'Kleur' : 'Couleur';
			include select.php.jade
			- $terms = get_terms( 'alcohol', 'orderby=term_order' );
			- $labelslug = 'alcohol';
			- $label = 'Alcohol';
			include select.php.jade
