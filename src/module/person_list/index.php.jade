.personlist
	ul.persons
		- if(d('sample',$data)):
			include person.jade
		- endif;
		- foreach ($dataperson as $keyperson => $slide):
			- $slide['itsadmin'] = $admin
			include person.jade
		- endforeach