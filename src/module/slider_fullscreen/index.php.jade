- $data = d('content',$data)
.flexslider
	ul.slides
		- foreach ($data as $keyslide => $slide):
			li
				img(src!="<?php echo d('img',$slide);?>")
				- if(!$admin):
				.warper.caption
					.flex-caption
						!=d('caption',$slide)
				- else:
					.flex-caption.editable
						!=d('caption',$slide)
				- endif;
		- endforeach